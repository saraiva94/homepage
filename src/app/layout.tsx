// Placeholder file to satisfy Next.js types in tsconfig.json
// This project uses Vite/React - see src/main.tsx for the actual entry point
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
