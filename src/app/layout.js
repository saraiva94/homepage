// Next.js type generation artifact compatibility.
// This Vite/React app does not use Next.js at runtime.

/** @param {{ children: any }} props */
export default function RootLayout({ children }) {
  return children;
}
